"use client";
import { useState } from "react";
import { PageShell } from "../components/SiteChrome";
import { formatCnyFromRub } from "../data/products";
export default function GiftCardPage() { const [amount, setAmount] = useState(3000); const [added, setAdded] = useState(false); return <PageShell><main className="gift-page"><section className="gift-visual"><div className="gift-card-art"><span>púsy</span><small>GIFT CARD</small><b>{formatCnyFromRub(amount)}</b></div></section><section className="gift-copy"><p>送一份选择的自由</p><h1>PÚSY 礼品卡</h1><div className="gift-amounts">{[1000, 3000, 5000, 10000].map((value) => <button className={amount === value ? "active" : ""} onClick={() => setAmount(value)} key={value}>{formatCnyFromRub(value)}</button>)}</div><p className="gift-desc">电子礼品卡会发送至收件人的邮箱，可用于在 PÚSY 官方商店选购喜欢的商品。</p><button className="gift-add" onClick={() => setAdded(true)}>{added ? "已加入购物袋" : "加入购物袋"}</button><details><summary>如何使用</summary><p>结账时输入礼品卡代码即可抵扣对应金额。礼品卡余额可分多次使用。</p></details></section></main></PageShell>; }
